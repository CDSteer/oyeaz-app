<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <% if current_user %>
      <%= f.label :commenter => current_user.name %><br>
    <% else %>
      <%= f.label :commenter %><br>
    <% end %>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

